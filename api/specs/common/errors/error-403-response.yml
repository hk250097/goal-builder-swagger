components:
  responses:
    403:
      description: "Invalid JWT"
      content:
        application/json:
          schema:
            $ref: "./error.yml#/components/schemas/error"
          example:
            code: "403"
            message: "Forbidden"
      headers:
        Content-Type:
          schema:
            type: "string"
            example: "application/json"