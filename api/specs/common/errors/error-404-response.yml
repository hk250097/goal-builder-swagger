components:
  responses:
    404:
      description: "Not Found"
      content:
        application/json:
          schema:
            $ref: "./error.yml#/components/schemas/error"
          example:
            code: "404"
            message: "Resource not found"
      headers:
        Content-Type:
          schema:
            type: "string"
            enum:
              - "application/json"