components:
  schemas:
    error:
      type: "object"
      required:
        - "code"
        - "message"
      properties:
        code:
          type: "string"
          description: "The error code (matches the http status code)"
        message:
          type: "string"
          description: "The error message"