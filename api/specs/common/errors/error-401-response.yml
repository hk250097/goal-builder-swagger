components:
  responses:
    401:
      description: "Invalid Roles or entitlements"
      content:
        application/json:
          schema:
            $ref: "./error.yml#/components/schemas/error"
          example:
            code: "401"
            message: "Unauthorized"
      headers:
        Content-Type:
          schema:
            type: "string"
            example: "application/json"
