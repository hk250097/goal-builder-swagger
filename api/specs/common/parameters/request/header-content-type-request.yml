components:
  parameters:
    header-content-type-request:
      in: "header"
      name: "Content-Type"
      required: "true"
      schema:
        type: "string"
        enum:
          - "application/json"